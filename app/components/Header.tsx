"use client";

import React from 'react';

const Header: React.FC = () => (
    <header style={{ padding: '10px 20px', backgroundColor: '#f8f9fa', textAlign: 'center' }}>
        <h5>TAN BEE LEOW</h5>
        <h1 style={{ fontSize: '48px' }}>Apply for ID card</h1>
    </header>
);

export default Header;
